# Roadmap

This roadmap is intentionally brief. Detailed work belongs in `spec/features/NNN-name/`.

## Candidate Features

- Improve AI provider selection and fallback behavior.
- Harden WebSocket command handling and error reporting.
- Expand canvas actions while preserving the `AIActionSchema` contract.
- Improve Android APK release flow without changing the dev app identity.
- Review PWA/service-worker cache behavior when frontend assets change.

## Source Notes

- Project context: `~/docs/agent-context/summaries/vozart.md`.
- Project rules: `AGENTS.md`.
